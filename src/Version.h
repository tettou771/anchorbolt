#pragma once

// The one version string: --version and the MCP serverInfo both read this.
// 0.8.x = feature-complete, pre-first-real-deployment; 1.0.0 once a real
// installation has run on it for a while.
constexpr const char* kAnchorboltVersion = "0.8.2";
